#include "opencv2/opencv.hpp"
#include "ToGray.h"

using namespace cv;

int main(int, char)
{
	/*
	Mat input = imread("input.jpg");
	if (input.empty())
		exit(1);

	ToGray tg;

	Mat gray = tg.toGray(input);
	imshow("ToGray image", gray);
	*/
	return 0;
}